require('../models/db');
const mongoose = require('mongoose');
const productModel = require('../models/products').productModel;
const userModel = require('../models/users').userModel;

const analyzeOrder = (req, res) => {
    // TODO: Move code in /index.js here
};
